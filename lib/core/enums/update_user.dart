/// In this specific enum, `UpdateUserAction`, this defining a set of actions
/// that can be associated with updating user information.
enum UpdateUserAction {
  email,
  displayName,
  bio,
  profilePic,
  password,
}
